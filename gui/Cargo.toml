[package]
name = "cosmic-scribe-gui"
version = "0.1.0"
edition = "2021"
description = "Cosmic Scribe — native UI window (Tauri spike)"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
cosmic-scribe = { path = ".." }
tauri = { version = "2", features = ["devtools"] }