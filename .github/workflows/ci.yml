name: CI

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy
      # Daemon crate only. Tauri/libcosmic need GTK/WebKit/COSMIC system packages.
      - run: cargo fmt --all -- --check
      - run: cargo clippy -p cosmic-scribe --all-targets -- -D warnings
      - run: cargo test -p cosmic-scribe
      - run: cargo build --release -p cosmic-scribe

  web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package-lock.json
      - run: npm ci
        working-directory: web
      - run: npm run lint
        working-directory: web
      - run: npm run build
        working-directory: web
